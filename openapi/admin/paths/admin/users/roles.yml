get:
  summary: 権限一覧取得
  tags: [管理者]
  security:
    - BeareAuth: []
  responses:
    "200":
      description: 権限ユーザー一覧
      content:
        application/json:
          schema:
            type: object
            properties:
              users:
                type: array
                items:
                  $ref: ../../../components/schemas/User.yml
    "400":
      $ref: '../../../components/responses/400BadRequest.yml'
