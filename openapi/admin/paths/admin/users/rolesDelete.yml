delete:
  summary: 権限削除
  tags: [adminUserRole]
  security:
    - BeareAuth: []
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: integer
  responses:
    "200":
      description: 削除結果
      content:
        application/json:
          schema:
            type: object
            properties:
              destroyAdminUserRoleResult:
                type: boolean
              message:
                type: string
                nullable: true
    "400":
      $ref: '../../../components/responses/Errors.yml#/400BadRequestError'
