Pagenage:
  type: object
  properties:
    current_page:
      type: integer
      example: 1
    from:
      type: integer
      example: 1
    last_page:
      type: integer
      example: 10
    per_page:
      type: integer
      example: 20
    to:
      type: integer
      example: 10
    total:
      type: integer
      example: 100
    first_page_url:
      type: string
      example: http://localhost:80/api/admin/users?page=1
    last_page_url:
      type: string
      example: http://localhost:80/api/admin/users?page=10
    next_page_url:
      type: string
      example: http://localhost:80/api/admin/users?page=2
    prev_page_url:
      type: string
      example: http://localhost:80/api/admin/users?page=1
    links:
      type: array
      items:
        type: object
        properties:
          url:
            type: string
          label:
            type: string
            example: "&laquo; 前"
          active:
            type: boolean
            example: false
    path:
      type: string
      example: http://localhost:80/api/admin/users
    data:
      type: array
      # example: []
